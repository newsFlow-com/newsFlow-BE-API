# BE-API 는 독립 DB 컨테이너를 띄우지 않습니다.
# PostgreSQL(5433) 과 Redis(6379) 는 newsFlow-BE-DATA 의 docker-compose 로 실행합니다.
#
# 실행 순서:
#   1. cd ../newsFlow-BE-DATA && docker-compose up -d
#   2. cd ../newsFlow-BE-API  && ./gradlew bootRun
services: {}
